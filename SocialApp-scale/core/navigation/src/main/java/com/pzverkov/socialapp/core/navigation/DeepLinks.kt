package com.pzverkov.socialapp.core.navigation

// Shared deeplink vocabulary. Features build their own URIs from these so the scheme and web host
// are declared once and a feature never hardcodes them.
const val DEEPLINK_SCHEME = "socialapp"
const val DEEPLINK_WEB_BASE = "https://socialapp.app"
